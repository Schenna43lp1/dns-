services:
  dns-checker:
    build: .
    container_name: dns-checker
    ports:
      - "8080:80"
    volumes:
      - dns_checker_data:/var/www/html/data
      - ./config.php:/var/www/html/config.php
    restart: unless-stopped

volumes:
  dns_checker_data:
